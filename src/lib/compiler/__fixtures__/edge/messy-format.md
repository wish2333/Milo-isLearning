# 混合格式文档

本文件刻意混合 HTML 标签、不规范标题层级与冗余空白，用于验证 Import Agent 的容错清洗能力（PRD FR-01 AC5）。

<h2>混乱标题层级</h2>

直接跳过 H2 用 H4：

#### 第四层标题

再跳回 H2：

## 嵌套 HTML

<div>
<p>这是被 HTML 标签包裹的段落。Import Agent 应剥离外层标签只保留文本内容。</p>
<p>但<strong>语义强调</strong>这类内联 HTML 应保留或转写为 Markdown 等价物。</p>
</div>

## 不规则空白



这里前面有 5 个空行。



这里是 tab	分隔的文本	还有更多 tab

## 混合列表

- 项目 1
* 项目 2（混用 - 与 *）
+ 项目 3（再混一个 +）

1. 有序列表
3. 序号不连续
5. 又跳一个

## 含脚本片段（应保留代码块结构）

```javascript
const x = 1
const y = 2
console.log(x + y)
```

## 结尾

如果 Import Agent 能把上面这份凌乱的内容整理成标题层级统一、空行规范的标准化文本，说明清洗逻辑健壮。
