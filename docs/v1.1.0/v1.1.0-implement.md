# V1.1.0 Implementation Progress

| 字段 | 值 |
| --- | --- |
| 文档版本 | 1.0 |
| 文档类型 | 实施进度追踪（基于 Sisyphus 编排会话） |
| 编写日期 | 2026-07-14 |
| 上游文档 | `docs/v1.1.0/v1.1.0-plan.md`（实施计划） |
| 代码基线 | commit `82e353c`（v1.0.1），branch `feat/v1.1.0-experience-polish` |
| 当前状态 | Phase A 完成，Phase B Wave 1 完成，Phase D 部分完成 |

---

## 1. 当前进度

### Phase A：可观测性与运维 — 100%（8/8 完成）

| 任务 | 状态 | 验证 |
| --- | --- | --- |
| PA.1: SQLite events 表 + schema v2 | COMPLETED | typecheck + 8 单测 |
| PA.2: events-repo.ts + /api/events 路由（安全加固） | COMPLETED | typecheck + 12 单测 + 安全审查 |
| PA.3: analytics.ts 双轨 flush + app_mode 注入 | COMPLETED | typecheck + 单测（含 showcase 零回归） |
| PA.4: runAgent() 返回 { data, usage } + pipeline.ts 12 调用点 | COMPLETED | typecheck + 单测 |
| PA.5: quality-report tokenUsage + EstimatedCost + QualitySummary 展示 | COMPLETED | typecheck + 单测 |
| PA.6: createAutoFixQuizBatch + MapperFixStats 4 计数器 | COMPLETED | typecheck + 9 单测 |
| PA.7: env fallback 埋点 events 表写入 | COMPLETED | typecheck + 单测 |
| Phase A 集成验证: typecheck 0 + test 513/513 + lint 0 | COMPLETED | 已验证 |

### Phase B：体验完善（核心功能）— 50%（4/9 完成，Wave 1 完成）

| 任务 | 状态 | 验证 |
| --- | --- | --- |
| PB.1: F04 SQLite compile_session + compile_staging 表 + compile-checkpoint.ts + 3 路由 | COMPLETED | typecheck + 20 单测（含 schema v3） |
| PB.2: F04 pipeline 改造（checkpoint 写入点 + resume 支持）+ SSE AbortSignal | **PENDING** | 依赖 PB.1 |
| PB.3: F04 客户端 resume UI（CompileResumePrompt + import 页检测） | **PENDING** | 依赖 PB.2 |
| PB.4: F07 Module 重命名（renameModule + renameCurrentModule + UI） | COMPLETED | typecheck + 单测（showcase 不可重命名） |
| PB.5: F22 主题进度恢复（exitSession/startSession + TopicProgress） | **PENDING** | 依赖 PB.6 domain.ts 类型 |
| PB.6: F23 聚合 mastery（domain.ts TopicMastery + topic-mastery.ts + UI） | COMPLETED | typecheck + 单测 |
| PB.7: F10 中文字体分包加载（cn-font-split + globals.css + scripts/split-fonts.ts） | COMPLETED | typecheck |
| PB.8: F13 重刷页学习时长（AttemptRecord answeredAt/timeSpentMs + review page UI） | **PENDING** | 依赖 PB.6 domain.ts 类型 |
| PB.9: F39 Non-UTF-8 编码检测（pipeline.ts validateInput U+FFFD） | COMPLETED | typecheck + 单测 |

### Phase C：工程工具链 — 0%（0/3 完成）

| 任务 | 状态 | 备注 |
| --- | --- | --- |
| PC.1: F30 Prompt 版本管理 | PENDING | 依赖 Phase B |
| PC.2: F31 英文 Prompt 模板 | PENDING | 依赖 PC.1 |
| PC.3: F32 LLM 响应录制 | PENDING | 依赖 Phase B F04 pipeline 改造 |

### Phase D：v1.0.0 衍生项 — 50%（2/4 完成）

| 任务 | 状态 | 验证 |
| --- | --- | --- |
| PD.1: D1 a11y 系统审计（axe-core + aria-label + focus-ring） | **PENDING** | 需全局组件扫描 |
| PD.2: D2 OG 图片资源（og-image.png + layout.tsx metadata） | **PENDING** | 需图片资源 |
| PD.3: D3 resetPreferences() action（settings-store + UI 按钮） | COMPLETED | typecheck + 单测 |
| PD.4: D4 openai-compat baseURL 运行时检测（env-fallback.ts null 返回） | COMPLETED | typecheck + 单测 |

---

## 2. 集成验证状态

| 检查项 | 结果 |
| --- | --- |
| `bun run typecheck` | 0 errors |
| `bun run test` | 556/556 passed（47 files） |
| `bun run lint` | 0 errors |

---

## 3. 已完成的新增文件清单

```
新文件（14 个）：
  src/lib/persistence/server/events-repo.ts          — events 仓库
  src/lib/persistence/server/compile-checkpoint.ts    — F04 checkpoint 仓库
  src/lib/runtime/topic-mastery.ts                    — F23 聚合 mastery 计算
  src/scripts/split-fonts.ts                          — F10 字体分包构建脚本
  src/app/api/events/route.ts                         — F11 events 端点
  src/app/api/compile/session/route.ts                — F04 创建 session
  src/app/api/compile/checkpoint/route.ts             — F04 写入 checkpoint
  src/app/api/compile/resume/route.ts                 — F04 读取 checkpoint
  src/lib/persistence/server/__tests__/schema-v2.test.ts
  src/lib/persistence/server/__tests__/events-repo.test.ts
  src/lib/persistence/server/__tests__/compile-checkpoint.test.ts
  src/lib/compiler/pipeline/__tests__/auto-fix-quiz-batch.test.ts
  src/lib/runtime/__tests__/topic-mastery.test.ts
  src/app/api/__tests__/env-fallback-telemetry.test.ts
```

---

## 4. 下一步推进指南

### 4.1 启动方式

```bash
cd Q:\Git\GithubManager\Milo-isLearning
git checkout feat/v1.1.0-experience-polish
```

### 4.2 验证基线

每次继续前运行确认代码状态基线：

```bash
bun run typecheck  # 必须 0 errors
bun run test       # 必须 556/556 passed
bun run lint       # 必须 0 errors
```

### 4.3 优先推进顺序

Phase B → Phase C → Phase D（残项）

#### 第一优先：Phase B Wave 2（PB.2 + PB.5 + PB.8）

这三个任务可并行派发（文件集不交叉）：

**PB.2（F04 pipeline 改造 + SSE AbortSignal）** — 最大任务
- 输入文件：`src/lib/compiler/pipeline/pipeline.ts`、`src/app/api/compile/route.ts`
- 依赖：PB.1 已完成（compile_session + compile_staging 表存在）
- 核心改动：
  1. `compileMarkdown()` 新增 `options?` 参数（`sessionId` + `resumeFrom`）
  2. Stage 4（module）和 stage 6.5（challenge）完成后调 `saveCheckpoint()`
  3. `resumeFrom` 存在时跳过已完成 stage，从 staging 表加载 artifact
  4. `/api/compile/route.ts` 加 AbortSignal（`req.signal`）
- 验证：编译到 stage 4 后刷新 → 从 stage 5 恢复
- 风险：R-H1（pipeline 回归），每次 checkpoint 改动后跑 E2E 基线对比
- 查看文件：`src/lib/compiler/pipeline/pipeline.ts` 的 `compileMarkdown()` 函数

**PB.5（F22 主题进度恢复）**
- 输入文件：`src/lib/state/topic-session-store.ts`、`src/lib/persistence/topic-library.ts`、`src/lib/persistence/shared/keys.ts`、`src/types/domain.ts`、`src/components/library/TopicCard.tsx`
- 依赖：PB.6 已完成（domain.ts 已有 TopicMastery 类型，需追加 TopicProgress）
- 核心改动：
  1. domain.ts 添加 `TopicProgress` 接口（`topicId + completedModuleIds + lastVisitedAt`）
  2. keys.ts 添加 `StorageKeys.topicProgress(topicId)` 键
  3. topic-session-store.ts：`exitSession` 写入快照，`startSession` 读快照合并
  4. TopicCard 显示"上次完成 X/Y 个模块"
- 验证：主题完成 2 个模块后退出 → 重新进入 → 前 2 个模块标记完成
- 查看文件：`src/lib/state/topic-session-store.ts` 的 `exitSession`/`startSession`

**PB.8（F13 重刷页学习时长统计）**
- 输入文件：`src/lib/state/attempts-store.ts`、`src/types/domain.ts`、`src/app/learn/review/[moduleId]/page.tsx`
- 依赖：PB.6 已完成（domain.ts 已有 TopicMastery，需追加 AttemptRecord 扩展）
- 核心改动：
  1. domain.ts `AttemptRecord` 加 `answeredAt?` 和 `timeSpentMs?` 字段
  2. Quiz 渲染时记录题目展示时间 → 提交时计算时长
  3. 重刷页显示总学习时长 + 平均每题时长
- 验证：重刷页显示"本次复习时长：X 分钟"
- 注意：两个任务（PB.5 + PB.8）都需要修改 domain.ts，不要并行派发——先完成 PB.8 的 domain.ts 改动，再做 PB.5，或者合并派发

#### 第二优先：Phase B Wave 3（PB.3）

**PB.3（F04 客户端 resume UI）**
- 输入文件：`src/lib/state/compile-job-store.ts` + `sessionId` 字段、`src/app/learn/import/page.tsx`、`src/app/learn/compiling/page.tsx`、`src/components/learn/CompileResumePrompt.tsx`（新建）
- 依赖：PB.2 完成后才可派发
- 核心改动：
  1. `compile-job-store` 加 `sessionId` 字段
  2. import 页加载时调 `/api/compile/resume` 检查 active session
  3. 有 active session 时显示 `CompileResumePrompt`（"检测到未完成的编译"）
  4. 用户选择"继续" → compiling 页带 `resumeFrom` 参数
  5. Showcase 模式不显示恢复提示

#### 第三优先：Phase D 残项（PD.1 + PD.2）

这两个任务与 Phase B/C 完全独立，可随时并行执行。

**PD.1（D1 a11y 系统审计）**
- 输入文件：全局组件扫描 + `e2e/a11y.spec.ts`（新建）
- 安装依赖：`@axe-core/playwright`
- 核心改动：
  1. 补全交互按钮的 `aria-label`（含蒙对标注、撤销、重命名按钮）
  2. `globals.css` 统一 `:focus-visible` 样式
  3. Playwright axe-core 扫描集成到 E2E
- 验证：axe-core 扫描 0 critical violations

**PD.2（D2 OG 图片资源）**
- 输入文件：`public/og-image.png`（1200x630） + `src/app/layout.tsx`
- 核心改动：`layout.tsx` metadata.images 添加 OG 图片引用
- 验证：分享到社交平台显示 OG 卡片

#### 第四优先：Phase C（PC.1-PC.3）

Phase C 依赖 Phase B 完成（F04 pipeline 改造为 F32 录制提供 hook 点）。

### 4.4 关键风险点

| 风险 | 触发条件 | 缓解措施 |
| --- | --- | --- |
| PB.2 pipeline 回归 | checkpoint 写入改变 pipeline 行为 | 每 stage 改动后跑 `bun run test` 对比 |
| PB.5 + PB.8 修改同文件 domain.ts | 并行派发时文件冲突 | 串行执行，或合并派发 |
| PB.10 字体文件缺失 | `scripts/split-fonts.ts` 找不到 SourceHanSerifSC | 字体文件需手动下载放入项目目录 |
| RB-M6 `.env.local` 环境陷阱 | 默认跑 production 模式 | `NEXT_PUBLIC_APP_MODE=showcase bun run dev` |

### 4.5 每次继续时需查看的关键文件

确认代码状态的快速检查点：

```bash
# 1. 当前分支确认
git branch --show-current   # 应为 feat/v1.1.0-experience-polish

# 2. 最新提交确认
git log --oneline -3

# 3. 工作区状态
git status --short

# 4. 快速验证
bun run typecheck && bun run test && bun run lint
```

核心文件查看顺序：
1. `docs/v1.1.0/v1.1.0-plan.md` — 确认当前阶段的详细规格
2. `src/types/domain.ts` — 检查 TopicMastery（PB.6）、AttemptRecord 等类型是否完整
3. `src/lib/persistence/server/schema.ts` — 确认 events/compile_session 表存在
4. `src/lib/compiler/pipeline/pipeline.ts` — 查看 totalUsage 累加器 + 待 PB.2 加的 checkpoint 点
5. 各任务对应的 test 文件确认测试覆盖度

### 4.6 版本交付验收标准

完成所有 Phase B/C/D 后运行：

```bash
bun run typecheck
bun run test
bun run lint
bun run e2e      # showcase 回归对比（基线 51 tests）
```

---

## 5. Phase B Wave 2 派发建议

Wave 2 可派发 3 个并行任务（PB.2 + PB.5 + PB.8）。文件冲突分析：

| 任务 | 修改文件 | 冲突风险 |
| --- | --- | --- |
| PB.2 | `pipeline.ts` + `compile/route.ts` | 无 |
| PB.5 | `topic-session-store.ts` + `topic-library.ts` + `keys.ts` + `domain.ts` + `TopicCard.tsx` | **与 PB.8 共享 domain.ts** |
| PB.8 | `attempts-store.ts` + `domain.ts` + `review/[moduleId]/page.tsx` | **与 PB.5 共享 domain.ts** |

**建议策略**：先串行完成 PB.8 或 PB.5 的 domain.ts 改动（小型改动，5 分钟），再并行派发剩下的逻辑。

或者将 PB.5 和 PB.8 合并为一个子任务由同一个 agent 完成（domain.ts 改动一次完成）。