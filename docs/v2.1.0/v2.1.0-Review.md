# V2.1.0 Review — 发布验证与学习闭环强化

> **V2.1.0 Review V1.5（P0 + P1.3 + P2.1 已完成）**
> 状态：P0/P1.1/P1.2/P1.3/P2.1 Complete，进入 P2.2（自动化门禁、真实 provider expand、SQLite/backup 验证、6 场景人工矩阵全部通过）
> 日期：2026-07-19
> 对应计划：[`v2.1.0-plan.md`](./v2.1.0-plan.md) V1.9
> 前序基线：V2.0.1（tag `v2.0.1` 已 push，772 tests 全绿）
> 关闭边界：P0、P1.1、P1.2、P1.3 与 P2.1 已关闭，进入 P2.2；P3 尚未开始

---

## 0. TL;DR（P0 已完成）

V2.1.0 P0 已完成：SQLite/backup 隔离、pre-existing E2E 修复、已答题数展示、Today/SearchDialog E2E、release gate、真实 provider expand、production backup/FSRS 验证与 A-F 人工矩阵均通过。

代码基线保持绿：typecheck / lint / 777 tests / 双模式 build / 全量 E2E 均通过。真实 provider 配置仅由用户在 `.env.local` 中提供，本次未读取该文件内容。

P0.6 Oracle 决策已锁定 5 项处理方案，影响 V2.1.0 P0/P1 的 scope（详见 §2）：项 3（已答题数）与项 5（Today/SearchDialog E2E）拉入 V2.1.0 P0；项 1（feynmanAttempt 持久化）作为 P1 前置；项 2（handleReenter 双按钮）与项 4（storage 统一）推迟 V2.0.2。

---

## 1. P0 任务逐项验收

### 1.1 P0.1 — SQLite 路径隔离 + 验证矩阵基础（✅ Done）

| 项 | 状态 | 关键变更 |
|----|------|---------|
| `ALC_DB_PATH` / `ALC_BACKUP_DIR` 覆盖 | Done | `src/lib/persistence/server/config.ts` 提供数据库与备份目录隔离，默认路径保持向后兼容 |
| 单元测试 | Done | `config.test.ts` 4 tests：默认 fallback、DB 覆盖、backup 覆盖、双开关 |
| `isolated-prod-env.ts` helper | Done | 自动分配端口，spawn `bun run dev`，轮询首页 ready，cleanup child/tmp DB/backup/cache |
| `v2.1-release-gate.spec.ts` | Done | showcase smoke + production SQLite backup/integrity smoke，已纳入全量 E2E |
| grep 验证无遗漏硬编码 | Done | `data/alc.db` 仅出现在 `config.ts:27`（fallback 默认值）和测试断言中；`db-singleton.ts` 通过 `SQLITE_DB_PATH` 间接引用无需改动 |

**Acceptance 满足**：DB 与 backup 路径均可覆盖；release gate 使用临时目录；真实 `data/alc.db` 与 `data/backup/` 未被本轮验证写入。

### 1.2 P0.2 — 验证 production SQLite + 自动备份（✅ Done）

> 执行日期：2026-07-19。隔离 production 环境、module/review 触发、backup rotation/WAL、integrity check 与失败写入保护均已人工确认通过。

| 验证项 | 状态 | 备注 |
|--------|------|------|
| 启动隔离 production server | ✅ | release gate helper 自动创建临时 DB/backup 目录并等待首页 ready |
| 强制备份 + integrity check API | ✅ | `/api/backup/auto {force:true}` 返回 `created:true`；`/api/backup/verify` 返回 `integrityCheck:ok`，路径位于临时目录 |
| module/review UI 触发规则 | ✅ 人工确认 | 先 flush、再按强制/24h 阈值触发 backup，未污染真实数据 |
| 失败写入任务不触发备份 | ✅ 自动覆盖 | `auto-backup-trigger.test.ts` 已验证存在 failed task 时跳过请求 |

### 1.3 P0.3 — 验证 FSRS replay + 真实 LLM expand（✅ Done）

> 执行日期：2026-07-19。provider 配置由用户提供；本次未读取 `.env.local` 内容。

| 验证项 | 状态 | 备注 |
|--------|------|------|
| FSRS replay/coordinator | ✅ 自动覆盖 | `fsrs-replay.test.ts`、`fsrs-schedule-coordinator.test.ts`、`fsrs-migrate.test.ts` 随全量测试通过 |
| 作答/蒙对/撤销/纠题/忽略/恢复 schedule | ✅ | 自动测试 + 人工 production 验证通过 |
| 真实 provider 配置 + 最小 Markdown expand | ✅ 人工确认 | expand → compile → backfill → `concept_intro` → 编辑保存链路通过 |
| Checkpoint/resume + 未匹配 anchor warning | ✅ 人工确认 | checkpoint/resume、anchor 回填与 warning/provider error 提示均已确认 |

### 1.4 P0.4 — pre-existing E2E 失败修复（✅ Done）

**实际根因（3 处，全部修复）**：

1. **Playwright `reuseExistingServer: true` 复用本地 production server**：用户本地若已启动 `bun run start` 的 production 构建服务器（port 3000），Playwright 会直接连上去。该服务器 `isShowcaseMode=false`，导致 `AppShell` 渲染 `StorageLoading`（"正在加载本地学习数据..."）永不水合，所有子页面被阻塞。**修复**：`playwright.config.ts` 端口 3000 → 3002，避免冲突。
2. **两个 `next dev` server 共享 `.next/` 缓存目录**：chromium (@3002) + chromium-showcase (@3001) 两个项目同时跑 `bun run dev`，共享 `.next/` 导致编译产物互相破坏，showcase 测试偶发失败。**修复**：`next.config.ts` 条件化 `distDir`（读 `E2E_CACHE_DIR` env），两个 server 分别用 `.next-3001/` `.next-3002/`。
3. **`FeynmanFinalView.handleSubmit` 在 `config` 为 null 时静默 return**：`Topic learning flow` 测试的 `beforeEach` 未注入 LLM config 到 localStorage，导致点击"提交评估"无反应（既不报错也不调 `/api/feynman-eval`），最终 timeout。**修复**：`e2e/topic.spec.ts` 的 Topic learning flow `beforeEach` 加 `addInitScript` 注入 mock `alc:settings`。

**修复涉及文件**：
- `playwright.config.ts`（端口 3002 + `testIgnore: /showcase/` + `E2E_CACHE_DIR`）
- `next.config.ts`（条件化 `distDir`）
- `e2e/topic.spec.ts`（Topic learning flow `beforeEach` 加 mock LLM config）
- `e2e/showcase/showcase-home.spec.ts`（删除 1 行冗余注释，无实质改动）
- `.gitignore`（加 `.next-*/`）
- `eslint.config.mjs`（加 `.next-*/**` 到 ignores，避免 lint 跑 E2E 临时构建目录产生 5386 个 false-positive）

**Acceptance 满足**：全量 E2E 48 passed / 8 skipped（production storage-layer 自跳过）/ 0 failed。V2.0.1 re-entry A/B 用例仍通过，无新回归。

### 1.5 P0.5 — 6 场景手动 production 验证矩阵（✅ Done）

> 执行日期：2026-07-19。用户在隔离 production/showcase 环境中人工执行；以下结果依据用户确认记录。**不能用脚本替代**（plan V1.3 §1.3）。

| 场景 | 步骤 | 预期 | 实际结果 |
|------|------|------|---------|
| A | 主题首次进入 → 做题 → 退出 → 再入 | 应保留 stage（V2.0.1 resumeModule） | ✅ 通过 |
| B | 主题入口做题 → 退出 → 题库"继续" | 应保留 stage（关键回归 S3） | ✅ 通过 |
| C | 题库"继续"做题 → 退出 → 主题入口 | 应保留 stage | ✅ 通过 |
| D | 模块完成后 TopicTransitionView "继续" | 下一个模块进度保留 | ✅ 通过 |
| E | 跳过模块后"重新进入" | 恢复 skipped 前的进度（若存在） | ✅ 通过 |
| F | 首页 Showcase 主题 → 做题 → 退出 → 再入 | 应保留（showcase 模式） | ✅ 通过 |

### 1.6 P0.6 — V2.0.1 已知项决策（✅ Done，Oracle 咨询）

详见 §2。

### 1.7 P0.7 — 正式 E2E 门禁收口（✅ Done）

> **当前结果**：全量门禁结果已填入 §3，P0.2/P0.3/P0.5 人工结果已补入本 Review；P0 正式关闭。

### 1.8 P1 前置 — `feynmanAttempt` 持久化（✅ Done）

| 项 | 状态 | 关键变更 |
|----|------|---------|
| `ProgressState` 兼容扩展 | Done | 增加可选 `feynmanAttempt` 字段；旧 snapshot 缺失时按 `null` 处理 |
| resume 恢复 | Done | per-module snapshot 与全局 Zustand blob 按 `updatedAt` 选胜者，并保留对应 Feynman 记录 |
| 双写同步 | Done | persist hydration 与 subscribe 均同步 `feynmanAttempt` |
| 生命周期清理 | Done | `startModule` / `reset` 清空旧记录 |
| 单测与门禁 | Done | progress-store 10/10；全量 74 files / 780 tests；typecheck / lint 通过 |

**Acceptance 满足**：模块中途退出后恢复时保留 Feynman 记录；历史 snapshot 平滑兼容；未改变既有 `done` 阶段重新开始语义。

### 1.9 P1.1 — 批量任务与 Module checkpoint（✅ Done）

| 项 | 状态 | 关键变更 |
|----|------|---------|
| 任务模型 | Done | 新增 `ExpandJob` / `ExpandJobItem` / `ExpandJobCheckpoint` 与状态、错误、attempt、sourceHash 字段 |
| 持久化 key | Done | 新增 `StorageKeys.expandJob` / `expandJobCheckpoint`，经 `StorageRepository` 读写 |
| 恢复与幂等 | Done | job index、checkpoint hydration、状态迁移保护、重复取消/清理幂等 |
| showcase 隔离 | Done | showcase 创建 transient job 但不落盘、不读取 production job |
| 单测与门禁 | Done | P1.1 6 tests；全量 75 files / 786 tests；typecheck / lint 通过 |

**Acceptance 满足**：刷新/重启后可从 item checkpoint 恢复，失败项保留 error 与 attempts，已完成/取消任务可清理且不影响进行中任务。

### 1.10 P1.2 — 批量 pipeline 与取消/恢复协议（✅ Done）

| 项 | 状态 | 关键变更 |
|----|------|---------|
| 串行 Module pipeline | Done | 每个 item 独立调用 `compileWithExpand`，完成后先写 Module/source，再写 done checkpoint |
| SSE 协议 | Done | 7 类 Topic Expand 事件加入 `CompileEvent` 联合，success/failure/pause/cancel 均有真实单测驱动 |
| pause/resume | Done | 只在 item 边界检查状态；resume 跳过 `done` item，失败项保留 error/attempts |
| sourceHash | Done | job 与请求 hash 不一致时返回 `409 source_changed`，pipeline 也拒绝直接恢复 |
| cancel API | Done | production-only、重复取消幂等；已完成 Module 与 checkpoint 不删除。in-flight item 采用边界收尾语义 |
| 单 Module 兼容 | Done | 既有 markdown/expand compile route 分支保持不变 |
| 单测与门禁 | Done | P1.2 6 tests；全量 76 files / 792 tests；typecheck / lint 通过 |

**Acceptance 满足**：已完成 Module 不因失败/取消丢失，暂停恢复只执行未完成项，7 类 SSE 事件有测试覆盖。

### 1.11 P1.3 — Topic UI 与导入结果（✅ Done）

| 项 | 状态 | 关键变更 |
|----|------|---------|
| production 批量入口 | Done | 导入页支持 Topic 名称、按行主题词、统一约束；showcase 隐藏该入口 |
| 任务视图 | Done | 新增 `ExpandJobView`，展示 job/item 状态、进度、attempts、错误与失败项重试 |
| pause/resume/cancel | Done | 接入 job control API；暂停后的旧 SSE 收尾后再恢复，避免连接竞态 |
| Module/Topic 落盘 | Done | 完成 Module 从 server KV 同步到 client storage，并加入既有 topic-library |
| 入口恢复约束 | Done | 进入学习统一使用 `enterModule({ moduleId, allowResume: true })` |
| 唯一身份 | Done | 批量生成 Module/source 使用唯一本地 ID，避免 item 间覆盖 |
| 单测与构建 | Done | 组件 4 tests；全量 77 files / 796 tests；typecheck/lint/双模式 build 通过 |

**Acceptance 满足**：用户可查看批量进度、暂停/恢复/取消并单独重试失败 Module；已完成 Module 可直接进入学习且保留恢复语义。

### 1.12 P2.1 — 统计趋势纯函数（✅ Done）

| 项 | 状态 | 关键变更 |
|----|------|---------|
| 7/30 日趋势 | Done | `computeStats` 输出按本地时区从旧到新的 `sevenDayTrend` / `thirtyDayTrend`，保持原有 7 日标量字段兼容 |
| 首答与题型分类 | Done | 按 slot 最早唯一 AttemptRecord 统计首答正确率，并区分 `newAttemptCount` / `reviewAttemptCount` |
| due 完成率 | Done | 基于当前 schedule 派生 due backlog；历史 due 语义显式保持为当前缓存可推导范围，不写入统计快照 |
| 数据边界 | Done | 覆盖空数据、无效 timezone、UTC/本地跨日、重复 attempt、忽略题与已删除 Module |
| 单测与门禁 | Done | stats targeted 8 tests；全量 77 files / 800 tests；typecheck / lint 通过 |

**Acceptance 满足**：趋势统计为纯函数派生结果，刷新前后不产生重复快照；固定时钟测试覆盖 7/30 日窗口及 due/首答语义。

---

## 2. P0.6 决策记录（Oracle 咨询结论）

| # | 已知不满足项 | 决策 | 理由 |
|---|------------|------|------|
| 1 | `feynmanAttempt` 在 `resumeModule` 清空 | **修在 V2.1.0 P1 前置** | P1 X2 批量扩充会生成大量新 module，每个都可能被中途退出后恢复。P1 之前修避免数据迁移范围扩大。最小改动：扩展 `ProgressState` schema + snapshot 持久化 feynmanAttempt + `resumeModule` 不硬置 null + 单测。注意：旧 snapshot 缺该字段时 graceful fallback 为 null（与当前行为一致）。Effort: Medium (1-2d) |
| 2 | `handleReenter` 不区分"继续"与"重做" | **推迟 V2.0.2** | 当前 `allowResume=true` 默认对 skipped 模块合理（用户跳过说明已有部分进度）。双按钮是纯 UX 决策，需要用户研究支撑。P1 不会显著增加 skipped 模块量。Effort if needed: <1h |
| 3 | "已答题数"不展示 | **修在 V2.1.0 P0**（顺手清掉） | 给纯函数加可选参数零风险（不传 = 原行为不变）。P1 批量扩充 UI 复用 `computeModuleProgress`，现在加比以后加成本低。不构成 scope creep。最小改动：`module-progress.ts` 加 `attemptsBySlot?` 可选参数 → UI 从 `useAttemptsStore` 读传入。Effort: <1h |
| 4 | per-module storage 二分（架构债） | **推迟 V2.0.2**（独立 milestone） | V2.1.0 P1 plan 已要求"兼容当前双写路径"（约束已计划）。统一 backend 失败模式是"用户进度丢失"（最高风险），需要数据迁移 + 回退方案 + 全量回归。与 P1 批量扩充交织会让复杂度指数级上升。Effort: Large (3d+) |
| 5 | Today review / SearchDialog 入口未加 E2E | **修在 V2.1.0 P0** | P0 定义就是"发布验证与回归"，E2E 缺口 IS P0 scope。Today review 和 SearchDialog 是 V2.0.0 核心用户入口。最小改动：新建 `e2e/today-review.spec.ts` + `e2e/search-dialog.spec.ts`。Effort: 2-4h |

### V2.1.0 Phase 归属总结

| Phase | 本项内容 |
|-------|---------|
| **P0**（发布验证，扩展） | 项 3（已答题数）+ 项 5（Today/SearchDialog E2E）已完成；P0.2/P0.3/P0.5 人工部分待补 |
| **P1 前置**（X2 之前） | 项 1（feynmanAttempt 持久化，1-2d） |
| **P1** | X2 批量扩充（checkpoint + pause/cancel/resume + SSE 协议），显式兼容双写路径 |
| **P2** | 学习趋势：P2.1 纯函数统计已完成，P2.2 Today/Stats UI 待进行 |
| **P3** | 搜索 benchmark |
| **→ V2.0.2** | 项 4（storage 统一，3d+）；项 2（handleReenter 双按钮，视用户 feedback） |

---

## 3. 验证结果（自动化门禁已通过）

### 3.1 静态检查（当前已通过）

| 命令 | 结果 |
|------|------|
| `bun run typecheck` | ✅ 通过（0 errors） |
| `bun run lint` | ✅ 通过（0 errors / 0 warnings，加 `.next-*/**` ignore 后） |

### 3.2 单元测试（当前已通过）

| 测试文件 | 用例数 | 状态 |
|----------|--------|------|
| `src/lib/persistence/server/__tests__/config.test.ts`（新建） | 4 | ✅ 全绿 |
| **完整测试套件** | ✅ | 74 files / 777 tests passed |

### 3.3 构建（✅ 已通过）

| 模式 | 命令 | 结果 |
|------|------|------|
| Showcase | `NEXT_PUBLIC_APP_MODE=showcase bun run build` | ✅ |
| Production | `NEXT_PUBLIC_APP_MODE=production ALC_STORAGE_BACKEND=sqlite bun run build` | ✅（提升权限后；sandbox 首次因 Google Fonts DNS 失败） |

### 3.4 E2E（✅ 全量通过）

| 用例类别 | 状态 | 备注 |
|---------|------|------|
| Showcase Home × 6 | ✅ 通过 | P0.4 修复后，端口隔离 + E2E_CACHE_DIR |
| Topic CRUD × 3 | ✅ 通过 | F1-6 文案变更已适配 |
| Topic learning flow | ✅ 通过 | P0.4 修复（mock LLM config 注入） |
| **re-entry preserves progress A/B** | ✅ 通过 | V2.0.1 新增；验证 P0 bug 已修复 |
| Review filter tabs × 2 | ✅ 通过 | 未受影响 |
| Smoke / API data / storage layer | ✅ 通过（部分 self-skip） | Production storage-layer 自跳过 |
| **全量汇总** | **48 passed / 8 skipped / 0 failed** | 含 P0 release gate、Today、SearchDialog；双模式项目均执行 |

---

## 4. 后续 Phase 影响（P0.6 决策对 P1/P2/P3 的传导）

1. **P1 前置新增**：`feynmanAttempt` 持久化必须在 X2 批量扩充之前修（决策项 1）。否则 P1 生成的每个新 module 都会触发费曼进度丢失问题，后续修需要迁移更多数据。
2. **P0 范围扩展**：决策项 3（已答题数）与项 5（Today/SearchDialog E2E）已在 P0 完成。
3. **P1 双写兼容明示**：决策项 4（storage 统一）推迟 V2.0.2，意味着 P1 必须显式兼容当前 LS per-module key + 全局 Zustand blob 双写路径。plan §P1 V2.0.1 集成约束已锁定。
4. **P2/P3 无影响**：学习趋势（P2）与搜索 benchmark（P3）独立于 P0 决策。

---

## 5. 与 V2.0.1 Review 的差异说明

> **重要**：V2.0.1 已 tag 发布（`v2.0.1` 推送到远程），其 Review 文档（`docs/v2.0.0/v2.0.1-Review.md`）作为历史快照**完全冻结**，本 V2.1.0 期间零修改。

V2.0.1 Review §3.4 表格记录的 E2E 失败现象为"费曼得分断言"，§3.5 列出 6 场景矩阵为"待人工验证"。**V2.1.0 P0 实际调查结论**：

- §3.4 失败现象已过时：实际根因是 Playwright `reuseExistingServer` 复用本地 production server + `.next/` 缓存冲突 + `FeynmanFinalView` config null 静默 return（详见本 Review §1.4）。V2.0.1 Review 的"费曼得分断言"描述是 git stash baseline 验证时的旧现象，2026-07-19 已不复现。
- §3.5 "待人工验证"已由 V2.1.0 P0.5 完成（见本 Review §1.5）；自动化 E2E 与人工结果分别记录。

**未来读者指引**：如需了解 V2.0.1 E2E 失败的实际根因与修复，请阅读本 Review §1.4，不要回查 V2.0.1 Review §3.4（那里的描述已被证明过时，但作为历史快照保留不修改）。

---
---

## 6. 修订记录

| 版本 | 日期 | 修订 | 作者 |
|------|------|------|------|
| 1.5 | 2026-07-19 | 完成 P2.1：新增 7/30 日趋势、首答正确率、new/review 分类、due 完成率与学习日统计；修复 legacy bucket 中 retry 被丢弃的兼容边界；全量 77 files / 800 tests、typecheck、lint 通过，进入 P2.2。 | Codex |
| 1.4 | 2026-07-19 | 完成 P1.3：Topic 批量入口、ExpandJobView、控制 API、Module/Topic 同步、唯一身份与入口恢复；全量 796 tests、typecheck、lint、双模式 build 通过，进入 P2.1。 | Codex |
| 1.3 | 2026-07-19 | 完成 P1.2：串行批量 pipeline、7 类 SSE 事件、pause/resume、sourceHash 校验、production cancel API；全量 792 tests、typecheck、lint 通过，进入 P1.3。 | Codex |
| 1.2 | 2026-07-19 | 完成 P1.1：批量任务模型、Module checkpoint repository、状态迁移保护、showcase 隔离；全量 786 tests、typecheck、lint 通过，进入 P1.2。 | Codex |
| 1.1 | 2026-07-19 | 完成 P1 前置：持久化并恢复 `feynmanAttempt`，兼容旧 snapshot；全量 780 tests、typecheck、lint 通过，进入 P1.1。 | Codex |
| 1.0 | 2026-07-19 | 用户确认真实 provider expand、production backup/FSRS 与 A-F 人工矩阵通过；P0 正式关闭。 | Codex |
| 0.2 | 2026-07-19 | 完成隔离 helper、backup/integrity release gate、Today/SearchDialog E2E、已答题数展示；全量门禁 48 passed / 8 skipped；真实 provider 与 A-F 人工矩阵仍待执行。 | Codex |
| 0.1 | 2026-07-19 | P0 进行中初稿。P0.1/P0.4/P0.6 完成；P0.2/P0.3/P0.5/P0.7 待办。含接班人提示 §6。 | Sisyphus（GLM-5.2 orchestrator） |
